# Database Configuration (Local Development)
DB_SERVER=localhost
DB_NAME=PodcastDB
CONNECTION_STRING=Server=localhost;Database=PodcastDB;Trusted_Connection=true;

# AWS Configuration (Production)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
S3_BUCKET_NAME=podcast-media-files-rb-2025

# AWS Parameter Store Parameters (Production)
# /podcast/db/server=your-rds-endpoint
# /podcast/db/name=PodcastDB